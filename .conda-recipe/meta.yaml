# source will be downloaded prior to filling in jinja templates
# Example assumes that this folder has setup.py in it
source:
  git_url: ../

package:
  name: droplet_planning_plugin
{% if GIT_DESCRIBE_NUMBER > '0' %}
  version: {{ GIT_DESCRIBE_TAG[1:] }}.post{{ GIT_DESCRIBE_NUMBER }}
{% else %}
  version: {{ GIT_DESCRIBE_TAG[1:] }}
{% endif %}

build:
  number: 0
  script: python -m mpm.bin.build

requirements:
  build:
    - microdrop >=2.4
    - pandas
    - path_helpers >=0.2.post4
    - pyyaml
    - si-prefix
    - wheeler.pygtkhelpers
    - zmq-plugin >=0.2.post2

  run:
    - microdrop >=2.4
    - pandas
    - path_helpers >=0.2.post4
    - pyyaml
    - si-prefix
    - wheeler.pygtkhelpers
    - zmq-plugin >=0.2.post2

about:
  home: http://github.com/wheeler-microfluidics/droplet-planning-plugin
  license: BSD
